@model SitefinityWebApp.Mvc.Models.CharityListViewModel

@using Telerik.Sitefinity.Modules.Pages;
@using Telerik.Sitefinity.Frontend.Mvc.Helpers;

@Html.Script(ScriptRef.JQuery, "top")

<div> 
    <div class="directory-container">


        <div id="A" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">A</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("A"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }

                }
            </ul>
        </div>
        <div id="B" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">B</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("B"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }

                }
            </ul>
        </div>
        <div id="C" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">C</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("C"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }

                }
            </ul>
        </div>
        <div id="D" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">D</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("D"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }

                }
            </ul>
        </div>
        <div id="E" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">E</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("E"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }

                }
            </ul>
        </div>
        <div id="F" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">F</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("F"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }
                }
            </ul>
        </div>
        <div id="G" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">G</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("G"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }
                }
            </ul>
        </div>
        <div id="H" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">H</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("H"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }
                }
            </ul>
        </div>
        <div id="I" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">I</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("I"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }
                }
            </ul>
        </div>
        <div id="J" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">J</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("J"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }
                }
            </ul>
        </div>
        <div id="K" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">K</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("K"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }
                }
            </ul>
        </div>
        <div id="L" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">L</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("L"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }
                }
            </ul>
        </div>
        <div id="M" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">M</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("M"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }
                }
            </ul>
        </div>
        <div id="N" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">N</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("N"))
                    {


                        <li><a href="@item.Url">@item.Name</a></li>

                    }
                }
            </ul>
        </div>
        <div id="O" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">O</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("O"))
                    {
                        <li><a href="@item.Url">@item.Name</a></li>
                    }
                }
            </ul>
        </div>
        <div id="P" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">P</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("P"))
                    {
                        <li><a href="@item.Url">@item.Name</a></li>
                    }
                }
            </ul>
        </div>
        <div id="Q" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">Q</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("Q"))
                    {
                        <li><a href="@item.Url">@item.Name</a></li>
                    }
                }
            </ul>
        </div>
        <div id="R" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">R</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("R"))
                    {
                        <li><a href="@item.Url">@item.Name</a></li>
                    }
                }
            </ul>
        </div>

        <div id="S" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">S</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("S"))
                    {
                        <li><a href="@item.Url">@item.Name</a></li>
                    }
                }
            </ul>
        </div>
        <div id="T" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">T</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("T"))
                    {
                        <li><a href="@item.Url">@item.Name</a></li>
                    }
                }
            </ul>
        </div>
        <div id="U" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">U</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("U"))
                    {
                        <li><a href="@item.Url">@item.Name</a></li>
                    }
                }
            </ul>
        </div>
        <div id="V" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">V</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("V"))
                    {
                        <li><a href="@item.Url">@item.Name</a></li>
                    }
                }
            </ul>
        </div>
        <div id="W" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">W</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("W"))
                    {
                        <li><a href="@item.Url">@item.Name</a></li>
                    }
                }
            </ul>
        </div>
        <div id="X" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">X</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("X"))
                    {
                        <li><a href="@item.Url">@item.Name</a></li>
                    }
                }
            </ul>
        </div>
        <div id="Y" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">Y</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("Y"))
                    {
                        <li><a href="@item.Url">@item.Name</a></li>
                    }
                }
            </ul>
        </div>
        <div id="Z" class="letter-container directory-item">
            <h2 style="padding-left:37px" class="letter">Z</h2>
            <ul style="list-style:none" class="directory-list">
                @foreach (var item in Model.Results)

                {

                    if (item.Name.StartsWith("Z"))
                    {
                        <li><a href="@item.Url">@item.Name</a></li>
                    }
                }
            </ul>
        </div>
    </div> 
    </div>

@Html.Script(Url.WidgetContent("Mvc/Scripts/Give-Script.js"), "bottom")